const [a, b, c, d] = [3, 6, 2, 13]

const soma = a + b + c + d 
const  subtracao = d - b
const mutiplicacao = a * b
const divisao = b / c
const modulo = 6 % 2

console.log(-soma, subtracao, mutiplicacao, divisao, modulo)